// model Embeddings {
//   id        String                @id @default(uuid())
//   text      String // The original text content
//   metadata  Json? // Flexible metadata storage (source, type, etc.)
//   embedding Unsupported("vector") // The vector embedding using pgvector

//   @@map("embeddings")
//   @@schema("ai")
// }
